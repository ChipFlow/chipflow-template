{
  "name": "ChipFlow Development Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },

  // Configure tool-specific properties
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/home/user/.venv/bin/python",
        "python.analysis.typeCheckingMode": "off",
        "python.linting.enabled": true,
        "python.linting.pylintEnabled": false,
        "python.linting.flake8Enabled": true,
        "python.formatting.provider": "black",
        "files.associations": {
          "*.toml": "toml",
          "*.py": "python"
        },
        "terminal.integrated.defaultProfile.linux": "bash",
        "terminal.integrated.fontSize": 14,
        "remote.autoForwardPorts": false,
        "github.copilot.enable": false,
        "github.copilot.inlineSuggest.enable": false,
        "chat.commandCenter.enabled": false,
        "chat.agent.enabled": false,
        "workbench.startupEditor": "readme"
      },
      "extensions": [
        "ms-python.python",
        "-github.copilot-chat"
      ]
    }
  },

  // Port forwarding disabled - users can manually forward ports if needed
  // "forwardPorts": [8000, 8080, 3000],

  // Run during prebuild - installs Python dependencies (cached!)
  "onCreateCommand": "bash .devcontainer/install-deps.sh",

  // Run post-create setup asynchronously (runs in background, doesn't block terminal)
  "postCreateCommand": "nohup bash /usr/local/share/post-create.sh > /tmp/post-create.log 2>&1 &",

  // Open README.md when container starts
  "postStartCommand": "code README.md",

  // This is needed to ensure README opens on attach as well
  "postAttachCommand": "code README.md",

  // Container optimizations
  "containerEnv": {
    "PYTHONUNBUFFERED": "1",
    "PYTHONDONTWRITEBYTECODE": "1",
    "PDM_VENV_IN_PROJECT": "1"
  },

  // Mount optimizations - persist venv and extensions
  "mounts": [
    "source=chipflow-vscode-extensions,target=/home/user/.vscode-server/extensions,type=volume",
    "source=chipflow-venv,target=${containerWorkspaceFolder}/.venv,type=volume"
  ],

  // Set container user
  "remoteUser": "user"
}
